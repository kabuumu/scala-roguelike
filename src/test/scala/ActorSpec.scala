import org.scalatest._

/**
  * Created by rob on 26/04/16.
  */
class ActorSpec extends FlatSpec with Matchers{
//  "Actor" should "update a GameState to move when the path is clear" in {
//    val pc = Actor(Position(3,3), 10, 10, isPC = true)
//    val npc = Actor(Position(3,4), 10, 10, isPC = false)
//    val state = GameState(Set(pc, npc), 10)
//
//    pc.update(Input(KeyCode.UP), state) should contain(GameState(Set(pc.copy(pos = Position(3,2)), npc), 10))
//  }
}
